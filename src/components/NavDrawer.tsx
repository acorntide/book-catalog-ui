/**
 * @file NavDrawer Component
 * Legacy component - no longer used. NavRail provides all navigation functionality.
 * Keeping file for reference but removing implementation to avoid compilation errors.
 */

export default function NavDrawer() {
    return null; // Component not used - NavRail handles all navigation
}
